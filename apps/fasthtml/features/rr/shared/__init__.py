"""Shared chrome bits for Research Radar — stage sub-nav + URL helpers.

Mirrors `features/dd/shared/` and `features/ycs/shared/` so the three peer
features pull their row-2 tab strips from the same shape."""
