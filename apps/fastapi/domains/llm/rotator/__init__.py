"""Rotator — bandit-routed free-tier LLM gateway. `chain` re-exported for
`chain.chat_judge_bandit_async`; `discovery` joins once its own §8 pilot lands.
"""
from __future__ import annotations

from . import chain
