"""LLM domain — rotator (bandit-routed free-tier gateway) + credentials + embeddings.
`rotator` re-exported for `llm.rotator.chain`; `credentials`/`embeddings` join
once their own §8 pilot pass lands.
"""
from __future__ import annotations

from . import rotator
