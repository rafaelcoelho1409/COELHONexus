"""Reusable Markdown memory bundles for the RR orchestrator — persist
ACROSS scans (vs the per-scan virtual filesystem in ../tools/state.py)."""
from __future__ import annotations

from . import service
