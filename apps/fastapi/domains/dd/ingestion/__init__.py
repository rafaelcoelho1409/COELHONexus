"""Ingestion domain — entry: runtime.dispatch.service.run(run_id, slug); tiers dispatched by catalog best_source.tier."""
from __future__ import annotations
from . import artifacts, filters, post, runtime, storage, tiers
