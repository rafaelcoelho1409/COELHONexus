{{- $appName := "web" -}}

apiVersion: v1
{{- include "coelhonexus.ServiceSettings" (dict "appName" $appName "root" .) | nindent 0 }}
{{- toYaml (index .Values $appName "portsSettings") | nindent 2}}
