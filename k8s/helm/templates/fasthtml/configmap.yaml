apiVersion: v1
{{- include "coelhonexus.ConfigMapSettings" (dict "appName" "fasthtml" "root" .) | nindent 0}}
data:
  {{- include "coelhonexus.commonEnvVars" . | nindent 2 }}
  # FastHTML server configuration
  PORT: "3000"
  # FastAPI backend URL (internal cluster DNS)
  FASTAPI_URL: "http://coelhonexus-fastapi:8000"
