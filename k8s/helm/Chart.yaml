apiVersion: v2
name: coelhonexus
description: coelhonexus
type: application
version: 1.0.0
appVersion: "1.0.0"

# No chart dependencies (2026-09-11) — COELHO LLM Rotator is always a
# separately-deployed service, never bundled here. See `llm.endpoint` in
# values.yaml. History: the `coelho-llm-rotator` subchart dependency (v1.1.7,
# `condition: coelho-llm-rotator.enabled`) was removed once the Settings-page
# endpoint field + its store-backed runtime override made the bundled copy
# redundant — it was silently drifting stale under `skaffold dev` because
# Nexus's own build never touched it. One URL field, no dual path.

